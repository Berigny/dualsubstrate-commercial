version: "3.8"

services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile.gateway
    ports:
      - "8080:8080"
    environment:
      UPSTREAM_GRPC: "dualsubstrate:50051"
    depends_on:
      - dualsubstrate
