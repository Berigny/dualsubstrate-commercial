version: "3.8"

services:
  api:
    build:
      context: .
      dockerfile: ops/Dockerfile
    environment:
      API_KEYS: "demo-key"
      EVENT_LOG_PATH: "/data/event.log"
    volumes:
      - ./data:/data
    ports:
      - "50051:50051"
    command: ["python", "-m", "api.grpc_server"]
