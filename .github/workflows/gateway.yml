name: Gateway

on:
  push:
    paths:
      - "cmd/gateway/**"
      - "proto/**"
      - "buf.yaml"
      - "buf.gen.yaml"
      - "buf.lock"
      - "Dockerfile.gateway"
      - "docker-compose.yml"
      - "docker-compose.gateway.yml"
      - ".github/workflows/gateway.yml"
  pull_request:
    paths:
      - "cmd/gateway/**"
      - "proto/**"
      - "buf.yaml"
      - "buf.gen.yaml"
      - "buf.lock"
      - "Dockerfile.gateway"
      - "docker-compose.yml"
      - "docker-compose.gateway.yml"
      - ".github/workflows/gateway.yml"

jobs:
  gateway:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Start stack
        run: docker compose -f docker-compose.yml -f docker-compose.gateway.yml up -d --build
      - name: Teardown
        if: always()
        run: docker compose -f docker-compose.yml -f docker-compose.gateway.yml down
